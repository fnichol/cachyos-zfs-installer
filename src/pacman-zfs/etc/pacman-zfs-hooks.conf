# ZFS Boot Environment Configuration for Pacman Hooks

# Number of boot environments to retain (default: 24)
RETENTION_COUNT=24

# ZFS pool and ROOT dataset path (no trailing slash)
ZFS_ROOT_POOL="zroot/ROOT"

# Note: Current BE is auto-detected from root mountpoint
